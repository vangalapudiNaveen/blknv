import { PricingArrayTypes } from "./PricingArraytypes";

export type PricingTypes = {
    title: string;
    subTitle: string;
    preTitle: string;
    ctmblock: string;
    cta: string;
    btnTitle: string;
    pricing: PricingArrayTypes[]
}